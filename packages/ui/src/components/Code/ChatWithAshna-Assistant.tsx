"use client"

import { Button } from "@workspace/ui/components/button";

const Run = () => {
  return (
    <Button className="bg-green-600 flex">
      Chat with Ashna 
    </Button>
  );
};

export default Run;
